/**
 * Имена семей проверки кода — один список на проверку и на сборщик набора.
 *
 * Заведено по счёту. Список жил дважды: в самой проверке (`check-code.mjs`)
 * и в сборщике набора (`kit.mjs`), который пишет новому проекту пустую базу.
 * Семьи прибавлялись в проверке, а база набора оставалась с четырьмя — то
 * есть новый проект получал файл, молчащий о половине того, что меряется.
 *
 * Отдельным файлом, потому что сборщику нельзя ВВОЗИТЬ проверку: она
 * запускается прямо при чтении, и сборка набора обернулась бы прогоном
 * проверки по чужому проекту.
 */

export const CODE_FAMILIES = [
  'twice', 'longFile', 'manyHooks', 'keep',
  'deadLink', 'translated', 'glued', 'mutSort',
]

/** Пустая база: ноль по каждой семье. На новом проекте долга нет, и первое
 *  же нарушение обязано валить сборку. */
export const emptyCodeBaseline = () =>
  Object.fromEntries(CODE_FAMILIES.map((k) => [k, 0]))
